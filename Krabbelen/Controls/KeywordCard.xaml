<UserControl x:Class="Krabbelen.Controls.KeywordCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
						 xmlns:modelviews="clr-namespace:Krabbelen.ViewModels" 
						 d:DataContext="{d:DesignInstance Type=modelviews:MainViewModel}"
						 mc:Ignorable="d" 
             d:DesignWidth="200">

	<Border
			Grid.Column="0"
			Grid.ColumnSpan="2"
			CornerRadius="10"
			Padding="3,1"
			Background="LightBlue"
			BorderBrush="Blue"
			BorderThickness="1"
			Margin="0 0 2 0">

		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<TextBlock
				Grid.Column="0"
				FontSize="10"
				HorizontalAlignment="Left"
				VerticalAlignment="Center"
				x:Name="KeywordText"/>

			<Button
				Grid.Column="1"
				Content="X"
				FontSize="8"
				Padding="1,0,1,0"
				Margin="3, 0, 3, 0"
				HorizontalAlignment="Right"
				VerticalAlignment="Stretch"
				Click="RemoveKeyword_Click"
				Name="RemoveKeyword"/>

		</Grid>
	</Border>


</UserControl>
